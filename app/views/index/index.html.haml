%table
  %tr
    %th Title
    %th Artist
    %th Album
    %th Track #
  - @tracks.each do |track|
    %tr{:class => cycle('normal', 'alternate')}
      %td
        = track.title
        = link_to "play", "/index/play/#{track.id}"
      %td= track.artist.title if track.artist
      %td= track.album.title if track.album
      %td= track.track_number